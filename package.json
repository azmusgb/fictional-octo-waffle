{
  "name": "workbench-studio-v7",
  "private": true,
  "version": "0.7.0",
  "scripts": {
    "dev": "npm --prefix client run dev",
    "build": "npm --prefix client run build",
    "test": "npm --prefix client run test"
  }
}
